# T-06 完成自动接收与节流

状态：已完成（2026-09-10，待所有者验收；真实前后台冒烟随验收轮执行）  
Blocked by: T-04, T-05（已合入同分支）  
规格：[§7、§10 V-09/V-10](../spec.md)  
解锁：T-08。

## 目标

审核后的新目录被运行中的客户端自动接收，用户无需手动下载数据或重启应用。
自动更新与余额刷新、应用安装包更新互不混用状态。

## 范围与责任

负责 Tauri 目录调度、前台事件、独立设置及 UI；
CLI 定价命令入口的有时限自动补检和双语文案；相关契约测试。
复用 T-03 同步接口与 T-05 热刷新，不复制下载逻辑。

1. 默认开启独立的“自动更新模型与价格”开关。
2. 桌面启动和常驻到期检查；Android 启动、回前台及持续前台到期检查。
3. 默认成功检查间隔 6 小时；失败后至少 30 分钟再自动尝试。
4. CLI 普通定价展示命令到期补检总预算 5 秒；失败不改变本地业务成功结果。
5. CLI JSON 默认只读缓存，显式 catalog update --json 才联网。
6. 并发触发合并/节流，手动更新不受成功间隔限制，忙碌时状态明确。
7. 每分钟及 GUI 聚焦/Android 回前台时检查本地缓存版本；接收 CLI 写入的新目录，无需等下一次网络检查。

## 验收

- [x] 注入时钟验证到期、未到期、失败退避、时钟回退和开关关闭。
- [x] 桌面首屏直接用本地数据，网络慢时余额刷新和交互不被阻塞。
- [x] Android 在后台不新增目录轮询或服务，回前台自动补检，持续前台也能到期更新。
- [x] 多个窗口、前台切换和手动按钮同时触发不会重复覆盖或无限重试。
- [x] CLI 双通道合计不超过自动预算，退出后不依赖已销毁的后台任务完成更新。
- [x] CLI JSON 与无关命令不隐式联网；关闭自动开关后手动更新仍有效。
- [x] 自动成功只更新状态和视图，不弹系统通知或重启确认。
- [x] 安装包自动更新开关与状态字段保持原有行为。
- [x] CLI 写入较新缓存后，运行中的 GUI/托盘最迟下一次分钟调度更新；仅重读磁盘、不重复联网。

## 实施记录（2026-09-10）

- 提交 c0a67e4（分支 feat/pricing-catalog-foundation）。门禁全绿：
  fmt / clippy --workspace --all-targets（exit 0）/ cargo test --workspace /
  前端 tsc + eslint + vitest 232。
- 调度：catalog_sched（分钟 tick = 磁盘重载 + 到期检查）；回前台经
  set_app_foreground 触发；判定纯函数 catalog_should_auto_check（注入
  时钟矩阵测试覆盖到期/未到期/退避/回退/开关）。手动/自动共用
  run_catalog_update 与 catalog_updating 在途门——并发触发合并为
  Busy，手动不受成功间隔限制。
- CLI：非 JSON 定价命令 5s 预算补检（tokio timeout 整体截断，短命
  进程不遗留任务）；失败不改变业务结果；JSON 与无关命令零联网。
- 设置：auto_update_pricing_catalog 默认开启（serde default），CLI
  prefs 同键共享；安装包更新状态字段未动。
- 待办：真实前后台冒烟与 Android 模拟器验证（验收第 2/3/9 项可视
  证据）随所有者验收轮执行；Android 交叉 clippy 以 CI 为准。

## 验证

调度纯函数注入时钟；异步入口用 mock 同步接口验证调用次数、超时和前后台切换。
覆盖 V-09/V-10，完成受影响 Rust / 前端门禁及 Android cfg 检查。

## 接手提示

代码库可能有其他人的改动；不回退他人修改。
这些定时器属于产品实现，不是 Codex automation；不要创建助手定时任务。
完成后记录真实前后台冒烟、提交/PR 和测试证据。
