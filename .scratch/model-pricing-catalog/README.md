# 模型与定价目录更新任务包

状态：规格与本地票据已建立，尚未实现。  
先读 [spec.md](spec.md)，接手说明见 [handoff.md](handoff.md)。

## 已确认目标

人工审核数据，审核合并后自动发布，客户端自动接收。
下架模型保留最后已知价格；未知模型不借用另一模型的价格。
本轮只准备 spec / tickets / handoff。

## 票据与依赖

票据 ID 是本地编号，不是 GitHub Issue 编号。
ready-for-agent 仅表示没有前置依赖，不表示已经开始实现或已获推送授权。

| ID | 票据 | Blocked by | 产出 |
|---|---|---|---|
| T-01 | [提取可加载目录与兼容接口](issues/01-catalog-foundation.md) | 无 | core 目录类型、离线种子、兼容入口与等价测试 |
| T-02 | [修正模型缺失与下架展示](issues/02-model-lifecycle.md) | T-01 | core 与 CLI 的正确模型身份和价格状态 |
| T-03 | [建立目录同步与持久缓存](issues/03-catalog-sync.md) | T-02 | core 更新接口、版本判定、回退与并发保护 |
| T-04 | [接入 CLI 目录命令](issues/04-cli-catalog.md) | T-03 | status / update 命令与缓存定价读取 |
| T-05 | [接入桌面和 Android 手动更新](issues/05-gui-catalog.md) | T-03 | 设置入口、目录快照、卡片/托盘刷新与草稿保护 |
| T-06 | [完成自动接收与节流](issues/06-auto-sync.md) | T-04、T-05 | 三端自动同步、独立开关和失败退避 |
| T-07 | [建立人工审核后的数据发布链](issues/07-reviewed-publication.md) | T-03 | 正式数据文件校验、变更报告、CI 和发布验证 |
| T-08 | [收敛入口并验收完整链路](issues/08-convergence-acceptance.md) | T-06、T-07 | 移除生产绕行、端到端证据、维护文档 |

## 任务图

~~~mermaid
flowchart LR
  T01["T-01 目录基础"] --> T02["T-02 模型生命周期"]
  T02 --> T03["T-03 同步与缓存"]
  T03 --> T04["T-04 CLI"]
  T03 --> T05["T-05 桌面 / Android"]
  T03 --> T07["T-07 审核发布"]
  T04 --> T06["T-06 自动同步"]
  T05 --> T06
  T06 --> T08["T-08 完整验收"]
  T07 --> T08
~~~

T-01 和 T-03 是跨端改造必需的 core 接口准备票，遵守先扩展、逐端迁移、最后收敛。
按项目约定，公开 API 改动先单独 PR 合入，后续调用端再跟进。
其他票据围绕可观察行为划分，包含所需的数据、逻辑、展示和测试。

图中没有依赖的分支可以在协调文件责任后交给不同实现者；
此处只描述依赖关系，本轮不派发实现代理。

## 共同完成条件

- 先写行为契约测试，再改实现；网络 mock、时钟注入、数据目录隔离。
- 对照 spec 的 V-01 至 V-12 验收，不以编译通过代替行为验证。
- Rust / 前端 / Android 分叉检查按 AGENTS.md 和改动范围执行。
- 新增、删除、职责变化通过 file-tree 技能维护；不手改 AGENTS.md 树块。
- 中文提交须有正文，走 PR；未经验收不推送，不绕过 hooks。
- 当前任务包仅为本地文档，未创建任何远端 Issue 或部署资源。
